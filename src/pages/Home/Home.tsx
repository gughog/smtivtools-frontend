import React from 'react';
import styles from './Home.module.css';

const Home = () => {
  return (
    <div className={`${styles.home__bg_main} ${styles.home__bg_dimensions}`}>
      Home Page
    </div>
  );
}

export default Home;
